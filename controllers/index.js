const {
    getCategories,
    getRecords,
    addRecord
} = require('./records')
const {
    getMonthChart,
    getCategoryChart
} = require('./charts')

module.exports = {
    getCategories,
    getRecords,
    addRecord,
    getMonthChart,
    getCategoryChart
}